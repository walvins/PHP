Funcion generarnumero(){


	return $num
}

funcion  heridos($num,$numMetido){
	//Los numeros que se repiten, cuenta 1, recorrer con for cada cifra y comparar con el metido
	return $numHeridos
}

funcion muertos($num, $numMetido){
	//Si esta en la misma posicion, cuenta 1	

	return $muertos
}